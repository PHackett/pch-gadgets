<?xml version="1.0" encoding="UTF-8" ?>
<Module>

	<ModulePrefs
		title="Recent Results"
		author="Paul Hackett"
		author_email="TRCCGadgets@paulhackett.com"
		description="For the rendering statistics for one year."
		thumbnail="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/images/TRCC-Shield.png"
		width="700"
		height="500"
		scrolling="true">
	</ModulePrefs>

	<UserPref name="linkTarget" display_name="Link URL target" datatype="string" default_value=""/>

	<Content type="html">

		<![CDATA[
			<!--
			Global CSS 
			-->
			<link href="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/css/trcc.css" rel="stylesheet" type="text/css">

			<!--
			Placeholder where the generated content will be placed 
			-->
			<div id="content_div"></div>

			<!--
			Include my utilities libraries
			-->
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/utils/GGUtils.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/utils/GGGadget.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/utils/GGMultiUrlLoader.js" type="text/javascript"></script>
			
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/utils/TRCCUtils.js" type="text/javascript"></script>
			
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/Fixture.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/MultiFixture.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/TeamsInYear.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/CricketMatch.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/MatchReport.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/Innings.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/BatsmanInnings.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/Extras.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/FOW.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/BowlerSummary.js" type="text/javascript"></script>

			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/PlayerStatsCollection.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/PlayerStats.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/BatsmanSummary.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/BowlerStats.js" type="text/javascript"></script>
			<script src="http://pch-gadgets.googlecode.com/svn/trunk/pch-gadgets/TRCC/objects/CatcherStats.js" type="text/javascript"></script>

			<script type="text/javascript">

				var gYear="";
				var gMultiLoader=new GGGadget_MultiLoader;
			
				//-------------------------------------------------[processSettings]-
				// Determine what year & team
				//--------------------------------------------------------------------
				function processSettings ()
				{
					var lPrefs=new _IG_Prefs();
			
					//
					// First the year - Any userPrefs?
					//
					gYear = TRCCUtils_ProcessPreferences ("year", lPrefs, "2007");
					
					//
					// Lastly - Save away the "link target"
					//
					GGUtils_setLinkTarget (lPrefs.getString ("linkTarget"));
				}
				
			
				//------------------------------------------------[processFixturesCB]-
				// From the fixtures, choose the latest results
				//--------------------------------------------------------------------
				function MatchDataCB (aMatchDataArray)
				{
					var lPSC = new GGTRCC_PlayerStatsCollectionO;
					var lH="<h1>Array size = " + aMatchDataArray.length + "</h1>";
					var lFailed="";
					var lFailedCount=0;
			
					//
					// Down all the game XML data
					//
					for (var lMatchIndex=0 ; lMatchIndex<aMatchDataArray.length ; ++lMatchIndex)
					{
						if (null == aMatchDataArray[lMatchIndex].mXML)
						{
							++lFailedCount;
							lFailed += "(" + lFailedCount + ") Failed to load URL " + aMatchDataArray[lMatchIndex].mURL + "<br>";
						}
						else
						{
							//
							// Parse XML to game object
							//
							var lCM = new GGTRCC_CricketMatchO (aMatchDataArray[lMatchIndex].mXML);
							var lIN;
			
							//
							// Process TRCC innings
							//
							if (null != (lIN = lCM.getTRCCInnings()))
							{
								//
								// Process the TRCC batting
								//
								for (var i=0 ; i<lIN.mBatsmen.length ; ++i)
								{
									lPSC.updateTRCCBatting (lIN.mBatsmen[i]);
								}
							}
			
							//
							// Process oppo innings
							//
							if (null != (lIN = lCM.getOppoInnings()))
							{
								//
								// Process the TRCC bowling
								//
								for (var i=0 ; i<lIN.mBowlers.length ; ++i)
								{
									lPSC.updateTRCCBowling (lIN.mBowlers[i]);
								}
								
								//
								// Process the TRCC catching
								//
								for (var i=0 ; i<lIN.mBatsmen.length ; ++i)
								{
									lPSC.updateTRCCCatching (lIN.mBatsmen[i]);
								}
							}
						}
					}

					//
					// If there were no games ...
					//
					if (0 == aMatchDataArray.length)
					{
						lH += "There are no results for the year " + gYear;
					}
					else
					{
						//
						// Get the batting stats HTML
						//
						lH += lPSC.batsmanHTML(false);
						lH += lPSC.batsmanHTML(true);
						lH += lPSC.bowlerHTML(false);
						lH += lPSC.bowlerHTML(true);
						lH += lPSC.catcherHTML();
					}
			
					if (0 != lFailed.length)
					{
						lH += "<br>" + lFailed;
					}
					else
					{
						lH += "<br>All games loaded successfully";
					}
			
					_gel('content_div').innerHTML = lH;
				}

				//------------------------------------------------[processFixturesCB]-
				// From the fixtures, choose the latest results
				//
				// @param aFixtures    Array of GGTRCC_FixtureO objects for this year
				//
				//--------------------------------------------------------------------
				function processFixturesCB (aFixtures)
				{
					//
					// Load the match data from the fixtures
					//
					gMultiLoader.mFinalCB = MatchDataCB;
			
					for (var i=0 ; i<aFixtures.length ; ++i)
					{
						if (aFixtures[i].mHasLink)
						{
							gMultiLoader.add (aFixtures[i].getMatchXMLURL());
						}
					}

					//
					// Load all the fixtures
					//			
					gMultiLoader.invoke();
				}
			
			
				//------------------------------------------------------[teamYearsCB]-
				// From the teams in year data, load the relevant fixtures
				//
				// @param  response  The XML giving the teams playing in this year
				//
				//--------------------------------------------------------------------
				function teamYearsCB (response)
				{
					if (response == null || typeof(response) != "object" || response.firstChild == null)
					{
						_gel("content_div").innerHTML = "<i>Invalid data.</i>";
						return;
					}
			
					//
					// Parse the data
					//
					var lTeamYears=new Array();
					
					GGTRCC_LoadTeamYearsFromXML (response, lTeamYears);
			
					//
					// We have the teams-by-year data - Now load the fixture data for
					// the year we are interrested in
					//
					var lFixA=new Array();
			
					for (var i=0 ; i<lTeamYears.length ; ++i)
					{
						if (lTeamYears[i].year() == gYear)
						{
							if (lTeamYears[i].haveSaturday())
							{
								var lF=new GGTRCC_FixtureTagO (lTeamYears[i].year(), "Saturday");
								lFixA.push (lF);
							}
				
							if (lTeamYears[i].haveSunday())
							{
								var lF=new GGTRCC_FixtureTagO (lTeamYears[i].year(), "Sunday");
								lFixA.push (lF);
							}
	
							if (lTeamYears[i].haveYouth())
							{
								//
								// Youth games do not count towards stats
								//
								// var lF=new GGTRCC_FixtureTagO (lTeamYears[i].year(), "Youth");
								// lFixA.push (lF);
							}
			
							if (lTeamYears[i].have2020())
							{
								//
								// 20-20 games do not count towards stats
								//
								// var lF=new GGTRCC_FixtureTagO (lTeamYears[i].year(), "20-20");
								// lFixA.push (lF);
							}
			
							//
							// We are done ...
							//
							break;
						}
					}
				
					//
					// Load the required fixture information
					//
					GGTRCC_loadMultipleFixtureLists (lFixA, _IG_FetchXmlContent, processFixturesCB);
				}
			

				//----------------------------------------------------------[display]-
				// Load the XML & call the renderer
				//--------------------------------------------------------------------
				function display()
				{
					//
					// Read the user prefs & hosting URL
					//
					processSettings();

					//
					// Load the fixtures for the given year
					//					
					GGTRCC_LoadTeamYearsXML (_IG_FetchXmlContent, teamYearsCB);
				}
				
				_IG_RegisterOnloadHandler(display);

			</script>
		]]>
	</Content>
</Module>
